apiVersion: v1
kind: Service
metadata: 
      name: cluster-chaitanya-service
      labels: 
        app: cluster-chaitanya-service 
spec:
  type: ClusterIP
  selector:
    app: chaitanya-app 
  ports:
    - port: 80
      #targetPort: 80
      name: nginx
      protocol: TCP
